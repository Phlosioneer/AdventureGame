{{!
Expects input "Event"

Event: {
    "title": "string",
    "description": "string",
    "options": [Card]
}
Card: {
    "title": "string",
    "description": "string",
    "buttonText": "string"
}

See qualityList.mustache for the definition of the Quality type.
}}
<header>{{title}}</header>
<p>{{description}}</p>
<br/>
{{#options}}
    <div class="card rounded">
        <header class="card-header">
            <p class="card-header-title">
                {{title}}
            </p>
        </header>
        <div class="card-content">
            <div class="columns">
                <div class="column is-flex">
                    {{description}}
                </div>
                <div class="column" style="flex-grow: 0;">
                    <button class="button">{{buttonText}}</button>
                </div>
            </div>
        </div>
    </div>
    <br/>
{{/options}}